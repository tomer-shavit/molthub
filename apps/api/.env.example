# Molthub API Configuration
DATABASE_URL=file:./dev.db
JWT_SECRET=your-jwt-secret-here-minimum-32-characters-long
PORT=4000
AWS_REGION=us-east-1

# Default deployment target (docker, local, kubernetes, ecs-fargate)
DEFAULT_DEPLOYMENT_TARGET=docker
